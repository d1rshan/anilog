{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@anilog/api": "workspace:*",
    "@anilog/auth": "workspace:*",
    "@elysiajs/cors": "^1.3.3",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "elysia": "^1.3.21",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@anilog/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
